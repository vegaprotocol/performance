CREATE TABLE results (
    TS DATE NOT NULL,
    VEGAVERSION TEXT NOT NULL,
    VEGABRANCH TEXT NOT NULL,
    TESTNAME TEXT NOT NULL,
    LPUSERS INTEGER,
    NORMALUSERS INTEGER,
    MARKETS INTEGER,
    VOTERS INTEGER,
    LPOPS INTEGER,
    PEGGED INTEGER,
    USELP INTEGER,
    PRICELEVELS INTEGER,
    FILLPL INTEGER,
    RUNTIME INTEGER,
    OPS INTEGER,
    EPS REAL,
    BACKLOG REAL,
    CORECPU REAL,
    DNCPU REAL
);
